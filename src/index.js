import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css'; // ملف CSS افتراضي
import App from './App.js'; // ملف App.js الافتراضي


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);


